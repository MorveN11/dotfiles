# Set cat
alias cat bat

# Set Zoxide
zoxide init fish | source
alias cd z

# Aliases
alias zwork "zellij attach work"
alias znazza "zellij attach nazza"
alias zserver "zellij attach server"
alias zfinder "zellij attach finder"
alias ls "eza --color=always --git --no-filesize --icons=always --no-time --group-directories-first"
alias vim nvim
alias catimg "wezterm imgcat"
